home

<%= form_for [current_user, @new_board] do |f| %>
  <%= f.text_field :name %>
  <%= f.submit 'Add' %>
<% end %>


<% @boards.each do |board| %>
  <p><%= link_to board.name, user_board_path(@user, board) %></p>
<% end %>
